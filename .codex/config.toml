# Project-local, stdio-only GitNexus MCP. The command resolves the exact
# devDependency recorded in bun.lock; it does not use npx or a global install.
[mcp_servers.gitnexus]
command = "bun"
args = ["run", "gitnexus:mcp"]

[mcp_servers.gitnexus.env]
GITNEXUS_HOME = ".gitnexus-home"
